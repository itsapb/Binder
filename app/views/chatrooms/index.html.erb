    <div class="main container">
    <h1 class="heading-profile text-center">Your messages 🛎️</h1>

<% current_user.chatrooms.each do |chatroom| %>
 <ul>
   <li>
   <div class="img-circle">

 <%
=begin%>
 <%= cl_image_tag user.photo.key, height: 120, width: 79, crop: :fill %>
<%
=end%>

    </div>
    <h2><%= link_to chatroom.name, chatroom_path(chatroom), class: "heading-dashboard" %></h2>
   <%= chatroom.messages.last.content if chatroom.messages.any? %>
  </li>
 </ul>
 <%end %>
</div>
