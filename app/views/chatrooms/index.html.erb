    <div class="main container">
    <br>
    <h1 class="heading-profile text-center">Your messages 🛎️</h1>
<br>
<% current_user.chatrooms.each do |chatroom| %>
 <ul>
   <li>
<div class="d-flex ">
    <% if current_user.photo.attached? %>
      <%= cl_image_tag current_user.photo.key, class: "avatar-large dropdown-toggle"%>
    <% else %>
      <%= image_tag "avatar.png", class: "avatar-large"  %>
    <% end %>
    
<div>
    <h2><%= link_to chatroom.name, chatroom_path(chatroom), class: "heading-dashboard" %></h2>
    <p><%= chatroom.messages.last.content if chatroom.messages.any? %></p>
</div>
</div>
  </li>
  <br>
 </ul>
 <%end %>
</div>
